# -*- coding: utf-8 -*-
"""Untitled20.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1OmSO6YbSygtwcuzuDhZoytcyJBsDpbf2
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

df=pd.read_csv('/content/drive/MyDrive/customer_churn.csv')
df.head(5)

df.info()

# Plot churn distribution
plt.figure(figsize=(5,4))
df['Churn'].value_counts().plot(kind='bar')
plt.title('Churn distribution')
plt.xlabel('Churn')
plt.ylabel('Count')
plt.tight_layout()
plt.show()

df=df.drop('CustomerID',axis=1)
df.head(2)

from sklearn.preprocessing import LabelEncoder
l=LabelEncoder()

df['Gender']=l.fit_transform(df['Gender'])

df['Gender']=l.fit_transform(df['Gender'])

df['Subscription Type']=l.fit_transform(df['Subscription Type'])

df['Contract Length']=l.fit_transform(df['Contract Length'])

from sklearn.model_selection import train_test_split
x = df.drop('Churn', axis=1)
y = df['Churn']
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2)
print(x_train.shape,y_train.shape)
print(x_test.shape,y_test.shape)

from sklearn.linear_model import LogisticRegression

model=LogisticRegression()

model.fit(x_train,y_train)

y_pred=model.predict(x_test)